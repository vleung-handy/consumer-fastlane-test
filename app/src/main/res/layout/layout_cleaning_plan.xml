<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"

    >

    <FrameLayout
        android:id="@+id/header_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        >

        <TextView
            android:id="@+id/text_view_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start|center_vertical"
            android:padding="@dimen/default_padding"
            tools:text="You have 1 active cleaning plan"
            />

        <Button
            android:id="@+id/button_plan_expand"
            style="@style/Button.Link.Bold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|center_vertical"
            android:gravity="end|center_vertical"
            android:text="@string/edit"
            />

        <ImageView
            android:id="@+id/image_plan_collapse"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|center_vertical"
            android:layout_marginEnd="@dimen/default_margin"
            android:layout_marginRight="@dimen/default_margin"
            android:clickable="true"
            android:src="@drawable/ic_pro_team_x_small"
            android:visibility="gone"
            />
    </FrameLayout>

    <include
        android:id="@+id/divider"
        layout="@layout/layout_divider"
        android:visibility="gone"
        />

    <LinearLayout
        android:id="@+id/container_plan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:animateLayoutChanges="true"
        android:orientation="vertical"
        android:visibility="gone"
        tools:visibility="gone"
        >

    </LinearLayout>

    <include
        layout="@layout/layout_divider"
        />

</merge>
