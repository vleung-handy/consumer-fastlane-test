<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Group ids are required if we want to show a divider between groups-->
    <group
        android:id="@+id/top_group"
        android:checkableBehavior="single"
        >
        <item
            android:id="@id/nav_menu_home"
            android:checked="false"
            android:icon="@drawable/menu_booking"
            android:title="@string/make_a_booking"
            />

        <item
            android:id="@id/nav_menu_my_bookings"
            android:checked="false"
            android:icon="@drawable/menu_my_bookings"
            android:title="@string/my_bookings"
            />

        <item
            android:id="@id/nav_menu_my_pro_team"
            android:checked="false"
            android:icon="@drawable/menu_pro_team"
            android:title="@string/my_pro_team"
            />

        <item
            android:id="@id/nav_menu_history"
            android:checked="false"
            android:icon="@drawable/menu_history"
            android:title="@string/history"
            />

        <item
            android:id="@+id/nav_menu_free_cleanings"
            android:checked="false"
            android:icon="@drawable/menu_referral"
            android:title="@string/free_cleanings"
            />

        <item
            android:id="@id/nav_menu_profile"
            android:checked="false"
            android:icon="@drawable/menu_account"
            android:title="@string/account"
            />
    </group>
    <group
        android:id="@+id/bottom_group"
        >
        <item
            android:id="@id/nav_menu_help"
            android:checkable="true"
            android:checked="false"
            android:icon="@drawable/menu_help"
            android:title="@string/help"
            />

        <item
            android:id="@id/nav_menu_log_in"
            android:checkable="false"
            android:icon="@drawable/menu_login"
            android:title="@string/log_in"
            />
    </group>
</menu>
