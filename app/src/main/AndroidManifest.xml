<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.handybook.handybook">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Base">

        <activity
            android:name=".ServiceCategoriesActivity"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".LoginActivity" />

        <activity android:name=".ServicesActivity" />

        <activity android:name=".BookingLocationActivity" />

        <activity android:name=".ProfileActivity" />

        <activity android:name=".BookingsActivity" />

        <activity android:name=".BookingOptionsActivity" />

        <activity android:name=".BookingDateActivity" />

        <activity android:name=".BookingAddressActivity" />

        <activity android:name=".BookingPaymentActivity" />

        <activity android:name=".BookingDetailActivity" />

        <activity android:name=".BookingConfirmationActivity" />

        <activity android:name=".BookingRecurrenceActivity" />

        <activity android:name=".BookingExtrasActivity" />

        <activity android:name=".BookingRescheduleOptionsActivity" />

        <activity android:name=".PeakPricingActivity" />

        <activity android:name=".PromosActivity" />

        <activity android:name="com.facebook.LoginActivity" />

        <meta-data android:name="com.facebook.sdk.ApplicationId"
                   android:value="@string/facebook_app_id" />

        <meta-data android:name="com.google.android.gms.version"
                   android:value="@integer/google_play_services_version" />
        <meta-data android:name="com.crashlytics.ApiKey" android:value="688486cdd4f0b095424122ad0a42e5e3473e83be"/>

    </application>

</manifest>
